# Copy to `.env` (gitignored) and fill in. Loaded automatically by the CLI via
# python-dotenv. Never commit real secrets.

# Groq API key used by the reasoning stage (mockbuilder/reasoning/reason.py).
GROQ_API_KEY=
