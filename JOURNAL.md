# MockBuilder Development Journal

## Phase 0: Foundations

### Date: 2026-06-30
* **Objective:** Initialize repository, define environments, and establish the AppModel schema contract.
* **Implementation Notes:**
  >
* **Challenges & Solutions:**
  >
* **Verification:**
  >

### Date: 2026-06-30
* **Objective:** Establish the AppModel schema contract in code: create the `mockbuilder` package, mirror the JSON Schema as Python dataclasses, and implement the validation gate.
* **Implementation Notes:**
  > Created the `mockbuilder/` package (`__init__.py`, `models.py`, `cli.py`).
  > `models.py` defines stdlib dataclasses mirroring every schema definition — `Meta`, `DesignTokens` (with `Typography`), `Entity` (with `EntityField`), `Component` (with `Prop`, `InteractiveElement`, `Action`, `Mutation`), `Screen` (with `Region`, `Variant`), `Flow` (with `FlowStep`), and the root `AppModel`. Each carries a `from_dict` classmethod for building typed objects from a validated payload.
  > `validate_app_model(data: dict) -> None` loads `app_model.schema.json` from disk and validates the dict with `jsonschema.validate`, raising `ValidationError` on the first violation.
  > `cli.py` uses `argparse` to expose `build <url> -o <out_dir>`, wired to a `run_build` stub that prints `Pipeline stubbed for <url> -> <out_dir>`.
* **Challenges & Solutions:**
  > The schema allows `additionalProperties` on `colors`, `spacing`, `radii`, and `shadows`. Stdlib dataclasses don't model open-ended key sets cleanly, so these are kept as plain `dict[str, str]` fields rather than forcing a rigid dataclass — fidelity to the contract over rigidity in the mirror.
  > **Architectural decision:** `app_model.schema.json` is the *single source of truth*. The dataclasses are a typed convenience mirror for the Python pipeline and are deliberately NOT used for validation — all contract checking goes through the JSON Schema via `validate_app_model`. This keeps one authoritative definition and avoids a second, drifting source of truth (and is why we chose stdlib dataclasses over Pydantic).
* **Verification:**
  > Imported `mockbuilder.models` and ran `validate_app_model` against a minimal payload (catches schema-violating dicts via `ValidationError`); exercised the CLI with `python -m mockbuilder.cli build <url> -o out` to confirm the stub prints as expected.

## Phase 1: Evidence Capture

### Date: 2026-06-30
* **Objective:** Stand up the crawler + recorder: a headless Playwright browser that visits a URL, captures a denoised structural snapshot of the DOM, derives a deterministic `state_hash`, and persists evidence (screenshot + actionable elements) to `evidence/`.
* **Implementation Notes:**
  > Created the virtualenv (`python -m venv .venv`), `pip install -e .`, and `playwright install chromium`.
  > `mockbuilder/crawler/dom.py` holds two browser-side captures run via `page.evaluate()`:
  >   - **DOM normalization** — clones `document.body`, removes all text nodes plus `<script>`/`<style>`/`<iframe>`/`<svg>` tags, and strips every attribute except `class`, `role`, and `data-testid`, returning the cleaned `outerHTML`.
  >   - **Element discovery** — queries `a, button, input, select, [role="button"], [role="tab"]` and returns `{tag, text, selector}` per element, where `selector` prefers `id`/`data-testid` and otherwise builds an `nth-of-type` path; `text` falls back through `innerText` → `value` → `aria-label`.
  > `mockbuilder/crawler/crawler.py` defines `async class Crawler` with `crawl(url, max_states=1)`: launches headless Chromium, `goto(url, wait_until="networkidle")`, runs both captures, hashes the normalized DOM with `hashlib.sha256().hexdigest()`, creates `evidence/` at the project root, and writes `evidence/{state_hash}.png` (full-page) and `evidence/{state_hash}_elements.json`. `max_states` is the seam for multi-state crawling later; only the landing state is captured now.
  > `cli.py`'s `build` command now calls `asyncio.run(Crawler().crawl(url))` instead of the stub. Added `evidence/` to `.gitignore`.
* **Challenges & Solutions:**
  > **Why hash the *structural* DOM?** Live apps are full of volatile noise — timestamps, ad/tracking markup, randomized class hashes, injected `<script>`/`<svg>`, A/B copy, per-request ids. If we hashed raw HTML, every reload would look like a brand-new state and the crawl graph would explode with near-duplicates. By first normalizing (drop text + noise tags, keep only the structurally meaningful attributes `class`/`role`/`data-testid`) and *then* hashing, two renders that are the *same screen* collapse to the *same* `state_hash`, while a genuinely different layout produces a different one. This makes the state hash the natural unique key (de-dupe, screenshot/elements filenames) and is what will keep multi-state crawling convergent rather than infinite.
  > The sandbox has no outbound network (`ERR_NAME_NOT_RESOLVED`), so end-to-end verification used a local `file://` HTML fixture instead of a public URL.
* **Verification:**
  > Crawled a local fixture containing noise (`<script>`, `<style>`, `<svg>`) and six actionable elements. Result: a stable 64-char `state_hash`, a full-page PNG, and an elements JSON with exactly 6 entries — `data-testid` selector used for the tagged button, `nth-of-type` paths for siblings, and the `aria-label` fallback correctly capturing the search input's label.

### Date: 2026-06-30
* **Objective:** Add the recorders (network fixtures + design tokens) and upgrade the crawler from a single capture to a breadth-first crawl over multiple states.
* **Implementation Notes:**
  > `mockbuilder/recorder/network.py` — `attach_network_listener(page, output_dir)` subscribes to `page.on("response")`, and for JSON-typed GET/POST responses writes the body to `output_dir/fixtures/{METHOD_URL}.json` (URL sanitized to a filesystem-safe, length-bounded key). Body reads are wrapped in try/except so a consumed/blocked stream degrades gracefully instead of aborting the crawl. Attached *before* navigation so no early payloads are missed.
  > `mockbuilder/recorder/design_tokens.py` — `harvest_tokens(page)` injects JS to read the computed `font-family`/`font-size`/`background`/`color` of `<body>` and the background-color of the structural landmarks (`header`/`nav`/`main`/`footer`), with a fallback that samples backgrounds from the first few generic `<div>`s. `save_tokens()` writes `evidence/design_tokens.json`.
  > `mockbuilder/crawler/crawler.py` — `crawl(url, max_states=3)` is now BFS. A queue holds *actions* (`{"url": base, "clicks": [selector, ...]}`); each state is reached by navigating to the base URL and replaying its click path on a fresh page (drift-free). New states (by normalized-DOM hash) get screenshot + elements + tokens persisted, then branch on up to 2 clickable elements (preferring `a`/`button`), each enqueued as `clicks + [selector]`.
* **Challenges & Solutions:**
  > **Network fixtures = the harness's seed database.** The generated mock is backend-free, so its data has to come from somewhere real. By recording the live app's actual JSON API responses to `fixtures/`, we capture authentic payloads (product lists, prices, categories) that the reasoning stage turns into the `entities[].seed` records in the AppModel and the generator emits as local data files. This is what makes the mock look real without a server — the seed is harvested, not invented.
  > **BFS + the structural hash prevents infinite loops.** A naive click-crawler loops forever: nav bars, "back home" links, and no-op buttons keep bouncing between a handful of screens. Because every state is keyed by the hash of its *normalized* DOM (§ Phase 1 first entry), revisiting a screen produces a hash already in `visited_states` and is silently dropped — so cycles collapse to a finite frontier and the crawl terminates. `max_states` is a second, hard ceiling on top of that.
* **Verification:**
  > Built three linked `file://` pages (home → products / about, each with a "back home" link) plus a no-op button on home. Crawl recorded 2 unique states (home depth 0, products depth 1); the no-op button and both back-home links hashed to the already-visited home state and were correctly *not* re-recorded — demonstrating the loop guard. `design_tokens.json` and the `fixtures/` dir were created (fixtures empty offline, since `file://` issues no JSON requests).

### Date: 2026-07-03
* **Objective:** Fix the token-harvesting gap exposed by legacy HTML: the harvester missed Hacker News's `#ff6600` because that color lives on a `<td bgcolor="#ff6600">`, not on a semantic landmark or a `<div>` with a CSS background.
* **Implementation Notes:**
  > Broadened the `harvest_tokens` JS (`recorder/design_tokens.py`) to also sample `table`/`td` elements alongside `header`/`nav`/`main`/`footer` and generic `<div>`s.
  > Added a `bgOf(el)` helper that prefers the presentational **`bgcolor` attribute** and falls back to the computed `background-color`, skipping transparent/default (`rgba(0,0,0,0)`, `transparent`). `bgcolor` values are normalized to canonical `#rrggbb` via `normalizeAttrColor()`, which handles hex with or without `#` (legacy `ff6600`) and named colors by round-tripping through the browser's own CSS color parser; computed rgb/rgba is likewise converted to hex.
  > Output now includes a deduped `structuralColors` array (in addition to the `structuralBackgrounds` dict) so a brand color is captured even when it's not the first element of its tag.
* **Challenges & Solutions:**
  > **Fidelity across web eras.** Computed styles alone are blind to pre-CSS presentation attributes that plenty of real sites (and most HTML email) still use. Reading `bgcolor` alongside computed `background-color` — and normalizing everything to hex — makes the harvester robust to both modern and legacy architectures, so the "extracted, not invented" design-token contract actually holds for sites like Hacker News. Using the browser to parse/normalize colors (rather than a hand-rolled table) keeps named colors and shorthand hex correct for free.
* **Verification:**
  > Ran the updated `harvest_tokens` against a local fixture mirroring HN's legacy markup (`<table bgcolor="#f6f6ef">` wrapping a `<td bgcolor="#ff6600">`). Result: `structuralBackgrounds` = `{"table": "#f6f6ef", "td": "#ff6600"}` and `structuralColors` = `["#f6f6ef", "#ff6600"]` — HN's orange and beige both captured as normalized hex, with the Verdana font stack preserved. (Live HN run needs network + `GROQ_API_KEY`; the cached model JSON stays deleted so the pipeline re-runs.)

## Phase 2: Reasoning

### Date: 2026-06-30
* **Objective:** Stand up the LLM client and the caching layer for the reasoning stage — turn per-state crawl evidence into a schema-valid AppModel, deterministically and cheaply.
* **Implementation Notes:**
  > `mockbuilder/reasoning/cache.py` — `get_cached_model(state_hash)` / `save_cached_model(state_hash, model_dict)`, a disk cache at `evidence/{state_hash}_model.json`.
  > `mockbuilder/reasoning/prompts.py` — `SYSTEM_PROMPT = ""` (filled in the next step).
  > `mockbuilder/reasoning/reason.py` — `async synthesize_model(evidence_dir, state_hash) -> dict`: checks the cache first; on a miss reads `{state_hash}_elements.json` and base64-encodes `{state_hash}.png`, sends both (image + elements JSON + "raw JSON only" instruction) to the async Anthropic client, parses the reply with `json.loads`, validates it with `validate_app_model`, and on failure appends the validation error to the message history and retries (up to 3 attempts). Only a schema-valid model is cached and returned.
* **Challenges & Solutions:**
  > **Caching on `state_hash` = determinism + cost control.** The LLM call is the one non-deterministic, paid step in the pipeline. Keying its output by the crawl's normalized-DOM hash means an unchanged screen never triggers a second call — identical evidence always resolves to the same `{state_hash}_model.json`. The cache is what makes the *whole* pipeline reproducible end-to-end, which matters more here than any per-call sampling setting.
  > **Validate-retry loop for hallucination mitigation.** LLMs happily emit JSON that looks right but violates the contract (missing required key, wrong enum, bad `data-testid` pattern). Rather than trust the first response, every candidate is run through the JSON-Schema gate (`validate_app_model`); a failure isn't fatal — the exact `ValidationError` is fed back into the conversation and the model is asked to fix it, up to 3 tries. This turns the schema into an automated corrector, so downstream stages only ever see contract-valid models.
  > **Two deviations from the brief, forced by current APIs (2026-06):** (1) The requested `claude-3-5-sonnet-20240620` is retired (404); substituted its documented drop-in `claude-sonnet-5`, pinned in the `MODEL` constant for a one-line swap. (2) `temperature=0` is rejected (400) on current Sonnet/Opus models — sampling params were removed — so it is omitted; the `state_hash` cache already provides stronger, exact determinism than `temperature=0` ever did (which never guaranteed identical outputs anyway). Streaming via `messages.stream().get_final_message()` is used so the 32K `max_tokens` budget (needed for seed-data-heavy models) doesn't hit SDK HTTP timeouts.
* **Verification:**
  > Offline (no network in sandbox): confirmed the package imports cleanly, `MODEL == "claude-sonnet-5"`, `SYSTEM_PROMPT == ""`, the cache round-trips a dict through `save`/`get` and writes `{hash}_model.json`, and the code-fence-stripping helper handles both fenced and bare JSON. The live LLM path (image → validated model) requires network/credentials and will be exercised on a first real run.

### Date: 2026-06-30
* **Objective:** Wire the reasoning stage into the CLI with proper secret handling: load credentials from a local `.env`, author the reasoning `SYSTEM_PROMPT`, and have `build` run crawl → reason on the primary state.
* **Implementation Notes:**
  > Added `python-dotenv` to `pyproject.toml` and installed it. `cli.py` now calls `from dotenv import load_dotenv; load_dotenv()` at the very top of the module (before argparse/pipeline imports), so a local `.env` populates the environment before any SDK client is constructed.
  > Filled `mockbuilder/reasoning/prompts.py` `SYSTEM_PROMPT` with the reasoning brief (expert UI/UX + systems-architect persona; raw-JSON-only output; entities with 4–8 deterministic seed rows; reusable components with role-derived `data-testid`s; mandatory `empty`/`error`/`loading` edge variants; declarative action wiring).
  > `run_build` now drives an async `_build_pipeline`: crawl the URL, take the primary (landing, depth-0) state, call `synthesize_model(EVIDENCE_DIR, state_hash)`, and log a one-line summary of the generated model (appName/appType + entity/component/screen/flow counts + output path).
  > Added `.env.example` documenting `ANTHROPIC_API_KEY` (the real `.env` stays gitignored).
* **Challenges & Solutions:**
  > **Secret handling via `python-dotenv`.** Credentials never appear in code or arguments. `AsyncAnthropic()` is constructed with no key and resolves `ANTHROPIC_API_KEY` from the environment; `load_dotenv()` at CLI startup ingests a local `.env` (which `.gitignore` excludes) if present, and is a no-op in environments where the variable is already exported (CI, containers). This keeps the trust boundary at the environment, not the source tree — `.env.example` documents the contract without carrying a value.
  > **Model target rules (why the requested model isn't used verbatim).** The brief named `claude-3-5-sonnet-20240620`, which is retired (404 as of 2026-02). The pipeline pins `claude-sonnet-5` — the documented drop-in replacement, same Sonnet tier — in the single `MODEL` constant in `reason.py`, so retargeting (e.g. to `claude-opus-4-8`) is a one-line change. Relatedly, the brief's `temperature=0` is rejected (400) on current Sonnet/Opus models, so it is omitted; determinism is provided by the `state_hash` cache instead.
* **Verification:**
  > Offline: `python -m mockbuilder.cli build --help` renders the parser; `import mockbuilder.cli` succeeds (confirming `load_dotenv()` and the reasoning imports resolve); `SYSTEM_PROMPT` is populated (1582 chars, contains "AppModel"); `python-dotenv` 1.2.2 imports. The end-to-end crawl → reason run still needs live network + `ANTHROPIC_API_KEY`.

### Date: 2026-07-03
* **Objective:** Pivot the reasoning provider from Anthropic Claude to **Groq (Llama 3.2 Vision)** for cost-effective, high-throughput, open-weights inference — and propagate the change through every file that referenced the old provider.
* **Implementation Notes:**
  > Swapped the dependency: `pip uninstall anthropic` / `pip install groq`; `pyproject.toml` now lists `groq` instead of `anthropic`.
  > Refactored `mockbuilder/reasoning/reason.py` to `AsyncGroq`: dropped all `anthropic` imports and the streaming/`_extract_text`/`max_tokens` plumbing, `MODEL = "llama-3.2-90b-vision-preview"`, client is `AsyncGroq()` (reads `GROQ_API_KEY` from env). Messages now use the OpenAI vision format — a `system` turn plus a `user` turn with a `text` part (elements JSON) and an `image_url` part carrying the screenshot as a `data:image/png;base64,...` URL. The call is `await client.chat.completions.create(model=MODEL, messages=messages, temperature=0)` and the reply is read from `response.choices[0].message.content`. The JSON-fence stripper, `validate_app_model` gate, and 3-try validate-retry loop are unchanged; retry feedback is appended as a `user` message with a `text` part.
  > Propagated the provider swap across the rest of the tree so nothing dangles: `cli.py` preflight now checks `GROQ_API_KEY` (was `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN`) with an updated error message; `.env.example` documents `GROQ_API_KEY`; `PLAN.md` tech-stack + phase notes now say Groq/Llama 3.2 Vision; the `app_model.schema.json` `modelVersion` example was updated to a Llama identifier. ("Claude Code" mentions in `PLAN.md` were left — those refer to the coding agent that builds this project, not the reasoning API.)
* **Challenges & Solutions:**
  > **Architectural pivot: Anthropic → Groq/Llama 3.2 Vision.** Moving reasoning onto an open-weights model served by Groq keeps the mock generator cost-effective and deployable without a dependency on an expensive proprietary API — the reasoning stage is now the cheap, fast, swappable part of the pipeline rather than its cost center. Because the AppModel contract is the boundary between reasoning and generation, the provider is fully interchangeable: any vision model that can emit schema-valid JSON works, and the `MODEL` constant is the only line that pins the choice. Determinism is now doubly assured — Groq accepts `temperature=0` (unlike current Claude models), *and* the `state_hash` cache still guarantees identical evidence → identical model.
  > **Message-format shift.** Anthropic's native content blocks (`{"type":"image","source":{...base64...}}` + top-level `system=`) became the OpenAI-style shape Groq expects (`system` role message + `image_url` data-URL part). Response extraction changed from iterating Anthropic content blocks to the single `choices[0].message.content` string.
* **Verification:**
  > Offline: `anthropic` uninstalled (import spec resolves to `None`); `groq.AsyncGroq` imports; `import mockbuilder.reasoning.reason` succeeds with `MODEL == "llama-3.2-90b-vision-preview"`; a repo-wide grep shows no `anthropic`/`ANTHROPIC` references remain outside the JOURNAL history; `app_model.schema.json` still parses; the `build` preflight now emits the `GROQ_API_KEY` guidance and exits 2 when the key is absent. Live crawl → reason still needs network + a real `GROQ_API_KEY`.
* **Follow-up (live run):** The first real run returned `400 model_decommissioned` — Groq has retired the `llama-3.2-*-vision-preview` ids. Confirmed the current vision lineup from Groq's docs and repointed `MODEL` to **`meta-llama/llama-4-scout-17b-16e-instruct`** (Llama 4 Scout: current open-weights Llama multimodal model, 128K context, cost-effective/high-throughput — the intended successor to Llama 3.2 90B Vision). The `modelVersion` schema example was updated to match. Alternative vision model on Groq if needed: `qwen/qwen3.6-27b`. No code changes beyond the `MODEL` constant — the OpenAI `image_url` message format is identical across Groq vision models.

### Date: 2026-07-03
* **Objective:** Get the reasoning stage to produce a schema-valid AppModel on a real target (Hacker News), and verify the result.
* **Implementation Notes:**
  > **Bug found on first correctness run:** Llama 4 Scout returned confident-but-wrong JSON — `screens` as an object instead of an array, an invented top-level `actions` key, missing `meta`/`designTokens`/`flows`, wrong component fields. Three retries couldn't recover.
  > **Root cause 1 — the schema was never in the prompt.** `SYSTEM_PROMPT` only *described* the AppModel in prose; a 17B open model can't infer the exact shape (arrays vs objects, required keys, field names) from prose. Fix: inject the actual `app_model.schema.json` text (`SCHEMA_TEXT`, loaded from `SCHEMA_PATH`) into the user turn, with an explicit note that `entities`/`components`/`screens`/`flows` are ARRAYS and which top-level keys are required/forbidden.
  > **Root cause 2 — retry feedback was self-defeating.** The loop fed `str(exc)` back to the model, and `str(jsonschema.ValidationError)` embeds the *entire* schema + instance (hundreds of lines). Added `_format_error()` to send a one-line targeted hint instead (`At $.screens: ... is not of type 'array'`).
* **Challenges & Solutions:**
  > **Show the contract, don't describe it.** For small open-weights models the decisive lever was putting the machine-readable schema in front of the model rather than a prose summary. Prose is enough for a frontier model; it is not enough for a 17B one. The concise-error change compounds this — each retry now gets actionable signal instead of noise.
* **Verification:**
  > `mockbuilder build https://news.ycombinator.com -o ./out` now completes: `appName='Hacker News'`, `appType='content-site'`, entities=1, components=2, screens=1, flows=1, cached to `evidence/{state_hash}_model.json`. Independently re-checked: the file passes `validate_app_model`, round-trips through `AppModel.from_dict()`, and is structurally faithful (a `story` entity with real titles from the screenshot; `StoryRow`/`Navbar` components with role-derived `data-testid`s; the `home` screen carries all four required variants — default/empty/error/loading).
* **Known quality gaps (schema-valid but not yet correct — tracked for follow-up):**
  > 1. **Dangling flow references.** Flow `browse-stories` points at `expectScreen: "story-detail"` (no such screen) and `testId: "story-{id}"` (matches no component element). The JSON Schema uses `$ref` for shape but does not enforce cross-references — this is exactly what the Phase-3 verifier's "no dead ends" assertion is meant to catch. Candidate earlier fix: a lightweight referential-integrity check inside the reasoning gate.
  > 2. **Invented design tokens.** `designTokens.colors.primary` came back `#333` (guessed gray) instead of Hacker News's brand orange `#ff6600` — because the crawler *harvests* `evidence/design_tokens.json` but the reasoning prompt never receives it, so the model invents tokens the schema says must be *extracted*. Fix: feed the harvested tokens into the prompt.
  > 3. **Fabricated `meta` fields.** `generatedAt` was hallucinated (`2023-12-01`); seed undershot (3 rows vs the requested 4–8). Fix: stamp `generatedAt`/`sourceUrl` deterministically in code rather than trusting the model, and nudge seed count.

### Date: 2026-07-03
* **Objective:** Close the three quality gaps above — use the harvested design tokens ("extracted, not invented"), force deterministic timestamps, and add referential-integrity rules to the prompt.
* **Implementation Notes:**
  > **Design tokens injected (gap #2).** `synthesize_model` now reads `evidence/design_tokens.json` into `tokens_json` (falling back to `"{}"` if absent) and appends it to the user turn: `... Here are the extracted design tokens:\n{tokens_json} ...`. `prompts.py` gains a 3rd provided-input line and a new **5. DESIGN TOKENS** block: "You MUST use the provided extracted design tokens. Do NOT invent colors or fonts. Map the extracted values to the semantic roles (primary, background, surface, text)."
  > **Programmatic timestamp (gap #3).** Immediately after `json.loads(...)` and before `validate_app_model(...)`, the code overrides `candidate["meta"]["generatedAt"] = datetime.datetime.now(datetime.UTC).isoformat()` (guarded so malformed output — missing/non-dict `meta` — still flows to the validator and the retry loop rather than raising `KeyError`). Added `import datetime`. The seed rule in prompt block 1 was hardened to "You MUST generate exactly 4 to 8 rows of `seed` data".
  > **Referential integrity (gap #1).** New prompt block **6. REFERENTIAL INTEGRITY**: flows may only navigate to screen IDs defined in `screens`, and every flow-step `testId` must exist in some component's `interactiveElements`. (Prompt-level guardrail; the Phase-3 verifier will still assert this mechanically.)
* **Challenges & Solutions:**
  > **Determinism belongs in code, not the prompt.** A timestamp is provenance, not reasoning — asking the model for it invites hallucination and breaks reproducibility. Overriding it in code makes `generatedAt` truthful and the run deterministic regardless of what the model emits.
  > **Harvester gap discovered while wiring gap #2.** Injection works, but the *evidence itself* is incomplete for Hacker News: `design_tokens.json` captured the correct font (`Verdana`) but **not** the iconic orange `#ff6600`. Reason: HN's orange bar is a legacy `<table bgcolor="#ff6600">` with no `<header>/<nav>/<main>/<footer>` landmarks and no `<div>` carrying the color, and `recorder/design_tokens.py` only samples those selectors plus generic `<div>`s. So the model can now faithfully use whatever we extract (Verdana, grays) but cannot map an orange it was never shown. **Follow-up:** broaden `harvest_tokens` to also sample table/`td` backgrounds and legacy `bgcolor` attributes (or pick the most prominent non-transparent background on the page) — the reasoning side is now correct; the extraction side needs to catch more.
* **Verification:**
  > Offline: `reason.py`/`prompts.py` import cleanly; the timestamp override yields a valid RFC3339 UTC string (e.g. `2026-07-03T09:12:27+00:00`); `SYSTEM_PROMPT` contains the new provided-input line, the DESIGN TOKENS and REFERENTIAL INTEGRITY blocks, and the "exactly 4 to 8" rule. Cleared the cached `{state_hash}_model.json` so the next live run regenerates. Live re-run expectation: `generatedAt` = current UTC and the `Verdana` font mapped through; the `#ff6600` check will remain unmet until the harvester gap above is fixed.
  > **Live outcome:** After the harvester fix (below), the real HN run produced `designTokens.colors.primary = "#FF6600"` and `background = "#f6f6ef"` (roles sane, body text dark — not orange), and `meta.generatedAt` = a fresh tz-aware UTC timestamp. **But** referential integrity via the prompt alone (block 6) did NOT hold: the flow still pointed at an undefined `expectScreen: "story-page"` and an undefined `testId: "story-link-{id}"` — the 17B model reasons locally and forgets to close the graph. This motivated the programmatic gate in the next entry.

### Date: 2026-07-03
* **Objective:** Convert referential integrity from a flaky prompt assertion into a **deterministic compilation gate** inside the reasoning loop, so dangling flow references can never survive.
* **Implementation Notes:**
  > Added `verify_graph_integrity(model) -> list[str]` to `reason.py`: it collects the defined `screens[].id` set and the defined component `interactiveElements[].testId` set, then walks every `flows[].steps[]` and returns a violation string for each `expectScreen` that isn't a defined screen and each `testId` that doesn't map to a defined element.
  > `{id}` placeholders are handled by `_testid_matches()`: a flow `testId` matches if it equals a component testId exactly (covers literal `login-btn` and literal `story-link-{id}`) or if it fits the pattern with `{id}` treated as a single `[A-Za-z0-9_-]+` segment (covers a resolved `story-link-5`).
  > Wired into the validate-retry loop **immediately after** `validate_app_model(candidate)`: if `verify_graph_integrity` returns violations, we `raise ValueError("\n".join(violations))`. The `except` tuple now includes `ValueError` (checked in `_format_error` *after* `json.JSONDecodeError`, which subclasses it), the rejection is printed to the terminal (`[reasoning] candidate rejected: ...`), and the exact violation text is appended to the message history so the model retries with a targeted fix.
* **Challenges & Solutions:**
  > **Schema shape ≠ graph correctness.** JSON Schema `$ref`s constrain *types*, not *cross-references* — it happily accepts a flow that navigates to a screen that doesn't exist. The 17B model kept exploiting exactly that gap (inventing a `story-page` screen it never declared) because prose rules don't bind it. Promoting the invariant to code makes it a hard gate: an inconsistent graph is now as impossible to emit as a schema-invalid one, and it reuses the existing retry machinery to self-correct rather than failing the build.
  > **Placeholder-aware matching.** The tricky part is that a flow may reference either the template `story-link-{id}` or a concrete `story-link-5`; naive string equality would false-negative the latter. Escaping the pattern and substituting `{id}` with a bounded wildcard matches both without over-matching unrelated ids.
* **Verification:**
  > Offline: ran `verify_graph_integrity` on the last real (known-bad) model — it flagged exactly the two dangling refs with the required wording (`... undefined screen: 'story-page'.` / `... undefined testId: 'story-link-{id}'.`). `_testid_matches` passes a truth table (`story-link-5` yes, `story-link-{id}` yes, `login-btn` yes, `story-page` no, `bogus-99` no). A hand-repaired model (adding the missing screen + element) returns an empty violation list. Cleared the cached model JSON so the next live run exercises the gate end-to-end (expected: terminal prints the rejection, retries, and converges on a model whose flows reference only `home` and declared elements).
  > **Live outcome:** the gate fired correctly on the real HN run (attempt 2 caught invented `story-page`/`comments-page` screens and retried) — but the build still failed after exhausting the 3-retry budget, because the model hit *independent* errors on different attempts and never cleared them all at once (attempt 1: an rgb color in a hex-only field; attempt 3: `repeatsOver: null`). The gate works; convergence needed the patch below.

### Date: 2026-07-03
* **Objective:** Convergence patch — stop the reasoning loop from thrashing between independent schema/integrity errors ("whack-a-mole") so it reliably passes all gates.
* **Implementation Notes:**
  > **Fix #1 — normalize harvester colors (`recorder/design_tokens.py`).** `bodyColor` and `bodyBackground` were still emitted as raw computed `rgb()`/`rgba()`; the model copied `rgb(130,130,130)` into a hex-only schema field and failed. Now both are converted to `#rrggbb` via the existing `rgbToHex`, and a transparent body background (`rgba(0,0,0,0)`) falls back to `#ffffff`. The token file no longer contains any non-hex color string for the model to copy.
  > **Fix #2 — headroom (`reasoning/reason.py`).** `MAX_RETRIES` 3 → 5, giving a 17B model room to resolve several independent errors sequentially.
  > **Fix #3 — forbid nulls (`reasoning/prompts.py`).** New prompt block **7. SCHEMA STRICTNESS**: "Never set optional fields to null. If an optional field does not apply, omit the key entirely." Targets the `repeatsOver: null` class of failure directly.
* **Challenges & Solutions:**
  > **Fix the input, not just the model.** The most durable of the three fixes is #1: the rgb-in-hex-field error was *self-inflicted* — we were feeding the model malformed evidence. Normalizing colors at the source removes the failure mode entirely rather than hoping the model corrects it on retry. #2 and #3 are complementary: more attempts (#2) plus one fewer way to fail (#3) shrink the search space the loop has to traverse. Together they convert a fragile loop that could diverge into one that converges — the input is clean, the illegal `null` move is forbidden, and there's budget to fix whatever remains.
* **Verification:**
  > Offline: the harvester now emits `bodyBackground: "#ffffff"`, `bodyColor: "#000000"` (fixture) / `#828282` (real HN gray) with the orange still captured; `MAX_RETRIES == 5`; `SYSTEM_PROMPT` contains block 7. Cleared the cached model JSON. Live `mockbuilder build https://news.ycombinator.com -o ./out` still needs network + `GROQ_API_KEY` (unavailable in this sandbox) — to be run on the host to confirm the loop completes.
  > **Live outcome:** the convergence patch worked — the real HN run completed (exit 0), converging on **attempt 4** (would have failed under the old 3-retry cap). Final model re-verified: schema valid, `verify_graph_integrity` → NONE, `generatedAt` a fresh UTC stamp, `colors.primary=#ff6600` / `background=#f6f6ef` / `text=#828282` (Verdana font), and the flow now closes properly — the gate pushed the model to *define* a `story-detail` screen (`home --[story-link-{id}]--> story-detail`) rather than dangle. The rgb-in-hex error never recurred (Fix #1).

## Phase 3: The React Generator

### Date: 2026-07-03
* **Objective:** Stand up the deterministic, template-driven React generator, starting with the global state manager (`GlobalContext`).
* **Implementation Notes:**
  > Added `Jinja2` to `pyproject.toml` and installed it (3.1.6). Created `mockbuilder/generator/` (+ `templates/`) with `__init__.py`.
  > `generator/templates/GlobalContext.jsx.jinja` — a React Context module: imports `createContext`/`useReducer`/`useContext`; `initialState` is injected by iterating `entities` and dropping each entity's `seed` (via Jinja's `tojson`) into a slice keyed by the entity name; a **generic reducer** switches on `action.type` (the schema's mutation ops — `add`/`remove`/`increment`/`decrement`/`set`/`clear`/`toggle`) against `action.store`; a `GlobalProvider` supplies `{ state, mutate }`; and a `useMockState()` hook exposes it (throwing if used outside the provider).
  > `generator/generate.py` — `ReactGenerator(app_model, output_dir)` builds a Jinja `Environment` with `FileSystemLoader(TEMPLATES_DIR)`, `autoescape=False` (output is JS/JSX, not HTML), and `trim_blocks`/`lstrip_blocks`. `generate_context()` renders the template with `entities=self.app_model['entities']` and writes `output_dir/src/GlobalContext.jsx`. No LLM/network calls — generation reads only the local templates dir.
* **Challenges & Solutions:**
  > **A generic reducer, not app-specific handlers.** The schema expresses interactions declaratively as `{op, store, payloadFrom}` (e.g. `op: 'add', store: 'savedStories'`). Rather than generate bespoke logic per app, one reducer keyed on `type`+`store` handles every mock's mutations deterministically — array ops tolerate a missing/non-array slice (lazy-init to `[]`), numeric ops coerce with `Number(...) || 0`, and stores referenced by mutations but not backed by an entity are created on first write. This keeps the boundary clean: the LLM fills the declarative contract, the generator mechanically compiles it, and the same AppModel always produces byte-identical code.
* **Verification:**
  > Rendered `GlobalContext.jsx` from the real HN AppModel into a temp dir: `initialState` carried the `story` (5 rows) and `comment` slices with real seed data, the reducer/provider/hook emitted intact, and output landed at `src/GlobalContext.jsx`. Confirmed the generator is purely template-driven (Jinja `Environment` bound to `templates/`, zero AI calls).

### Date: 2026-07-03
* **Objective:** Add the component and screen generators — turn `components[]` and `screens[]` into routed React files with edge-state switching and instance-level test ids.
* **Implementation Notes:**
  > `templates/Component.jsx.jinja` — emits `export default function {{ component.name }}(props)`. Each `interactiveElement` renders as a tag chosen by `kind` (`link`→`a`, `input`/`select`→those, else `button`); the `useMockState` hook and a `mutate({ type: op, store, payload })` handler are wired **only when** the element declares an `action.mutation` (`payloadFrom` of `boundEntity`/absent → the whole `props` record, else `props.<field>`). `data-testid` is attached to every element; a `{id}` placeholder becomes a template literal `data-testid={\`story-link-${props.id}\`}`, otherwise a static string.
  > `templates/Screen.jsx.jinja` — imports `useSearchParams` from `react-router-dom`; reads `?state=` and early-returns a `data-testid="{kind}-state"` block for every declared non-`default` variant; the happy path iterates `layout.regions`, maps region names to semantic tags (`sidebar`→`aside`, `modal`→`div`, others 1:1), and places each region's components. A region with `repeats`/`repeatsOver` maps over `state[<entity>]` (spreading each record into the component, keyed by `id` in a `Fragment`); `useMockState`/`Fragment` are imported only when a repeat region needs them. The kebab `screen.id` is PascalCased for the function name (`story-detail` → `StoryDetail`); the file keeps the kebab id (`story-detail.jsx`).
  > `generate.py` — registered a deterministic `label_expr` Jinja filter (mirrors the `{id}` rule for labels: `"{title}"` → `` `${props.title}` ``, plain text → a quoted string). Added `generate_components()` (→ `src/components/{Name}.jsx`) and `generate_screens()` (→ `src/screens/{screenId}.jsx`), and a `generate()` pipeline that runs context → components → screens in order.
* **Challenges & Solutions:**
  > **`?state=` query switch = deterministic edge-state coverage.** Rather than needing special data or backend conditions to reach an empty/error/loading screen, every declared variant is reachable by a URL query (`?state=error`). The generator compiles one early-return per variant, so edge-state completeness (declared in the AppModel) becomes mechanically reachable and testable — an agent can force any state on demand, and the verifier can assert each one.
  > **`{id}`/`{field}` interpolation = real instance-level hooks.** Schema testIds/labels carry `{id}`/`{field}` placeholders; the generator rewrites them into JS `${props.id}` / `${props.field}` inside template literals, so a repeated `StoryRow` yields per-instance addressable ids (`story-link-5`, `story-link-6`, …) and renders the actual record data — turning declarative placeholders into concrete, deterministic DOM without any per-app code.
* **Verification:**
  > Ran the full pipeline on the real HN AppModel into a temp dir → `src/components/StoryRow.jsx` + `CommentRow.jsx` and `src/screens/home.jsx` + `story-detail.jsx`. `StoryRow.jsx` carries `data-testid={\`story-link-${props.id}\`}` and interpolated labels (`${props.title}`, `${props.points}`); `home.jsx` embeds the `useSearchParams()` → `empty`/`error` early-returns and maps `state['story']` into keyed `StoryRow`s; the non-repeating `story-detail.jsx` correctly renders a bare `<StoryRow />` and omits the unused `Fragment`/`useMockState` imports. `label_expr` unit-checked across placeholder / plain / mid-string cases.

### Date: 2026-07-03
* **Objective:** Complete the generator — emit the app shell (router + Vite/Tailwind scaffold + entry points) so the output is an installable, runnable project, and wire generation into the `build` command.
* **Implementation Notes:**
  > `templates/App.jsx.jinja` — imports `BrowserRouter`/`Routes`/`Route` and `GlobalProvider`, dynamically imports each screen (PascalCased id → `./screens/{id}`), and builds one `<Route path={route} element={<Screen/>} />` per screen inside `<GlobalProvider>`. A new `route_path` Jinja filter rewrites schema route placeholders to React Router syntax (`/story/{id}` → `/story/:id`).
  > Config templates: `package.json.jinja` (react/react-dom/react-router-dom deps; @vitejs/plugin-react + vite + tailwindcss + postcss + autoprefixer devDeps; `type: module`; slugified package name from `meta.appName` via `package_slug`), `vite.config.js.jinja` (React plugin), `tailwind.config.js.jinja` (theme.extend.colors seeded from `designTokens.colors` — `primary`/`background`/`surface`/`text`), `index.html.jinja` (`#root` mount + `/src/main.jsx`), `main.jsx.jinja` (React 18 `createRoot` mounting `<App/>`). Also added the two files Tailwind can't function without — `postcss.config.js.jinja` and `src/index.css.jinja` (the `@tailwind` directives) — since a `tailwind.config.js` with no PostCSS pipeline is inert.
  > `generate.py` — added a `_write(template, relpath, **ctx)` helper and `generate_scaffold()` writing all root config + `src/{main.jsx,index.css,App.jsx}`; the `generate()` pipeline now runs context → components → screens → scaffold. Registered the `route_path` filter and `package_slug` helper.
  > `cli.py` — the `build` command now invokes `ReactGenerator(model, out_dir).generate()` after reasoning, so `-o <dir>` (unused since Phase 1) finally produces the harness end-to-end.
* **Challenges & Solutions:**
  > **Dynamic route mapping from the blueprint.** Routes are compiled straight from `screens[].route`, so the URL surface of the mock mirrors the source app's structure with zero hand-wiring — and because path params in the schema use `{id}` while React Router expects `:id`, one deterministic rewrite bridges the two. The screen id is the single source of truth for three things at once: the file name (`story-detail.jsx`), the import identifier (`StoryDetail`), and the route element — so they can never drift.
  > **The scaffold makes the output a *project*, not just files.** Prior phases emitted components/screens that assumed a provider and a router that didn't exist. `App.jsx` supplies both (wrapping everything in `GlobalProvider` + `BrowserRouter`), and the Vite/Tailwind config turns the tree into something `npm install && npm run dev` can boot — with the brand palette flowing from the crawl's design tokens all the way into Tailwind's theme.
* **Verification:**
  > `mockbuilder build https://news.ycombinator.com -o ./out` (exit 0) produced a full project tree: `package.json`, `vite.config.js`, `tailwind.config.js`, `postcss.config.js`, `index.html` at the root; `src/{App.jsx,main.jsx,index.css,GlobalContext.jsx}` + `src/components/*` + `src/screens/*`. `App.jsx` maps `/` → `<Home/>` and `/story/:id` → `<StoryDetail/>` inside `GlobalProvider`; `tailwind.config.js` carries `primary:#ff6600 / background:#f6f6ef / surface:#ffffff / text:#828282`. **`cd out && npm install` completed cleanly — added 133 packages, exit 0, no `ERESOLVE`/peer conflicts** (only routine `npm audit` advisories).
* **Bug fix — Jinja/JSX double-brace collision (`npm run dev` crash).** The generated `GlobalContext.jsx` failed to compile: `<GlobalContext.Provider value=(Undefined, Undefined)>`. Cause: the template line `value={{ state, mutate }}` was meant as a JSX object literal (`value={ { … } }`), but Jinja parsed the doubled braces as its own `{{ … }}` output tag and evaluated `state, mutate` as undefined Jinja globals. (It slipped through because the earlier render check only printed the first 30 lines, not the Provider at line 66.) **Fix:** hoist the object to a named `const contextValue = { state, mutate }` and use single-brace `value={contextValue}` — no doubled braces, no collision. Swept all templates for the risky `={{` (JSX object-literal attribute) pattern; none remain. **Re-verified live:** started the Vite dev server against `./out` and force-compiled the whole module graph over HTTP — `main.jsx`, `App.jsx`, `GlobalContext.jsx`, both screens, and both components all returned HTTP 200 and transformed with no `Transform failed`/`JSX value should be` errors; the root document served 200. The generated mock now boots.

### Date: 2026-07-03
* **Objective:** Make the generated mock genuinely interactive and styled — and, critically, make interactivity *not depend* on the free-tier LLM populating every element `action`.
* **Implementation Notes:**
  > **Interactivity + styling (templates).** `Component.jsx.jinja` now wires `useNavigate` for `navigate`/`navigateAndMutate` actions and `useMockState().mutate(...)` for `mutateState`, resolving `action.targetScreen` (kebab id) to a real route via a `screen_routes` map + `nav_target` filter (`{id}`→`${props.id}`). Elements render as clean Tailwind rows (`flex items-center gap-4 py-3 px-4 border-b border-gray-100`) with dark title links, subtle gray action buttons, and hover states. `Screen.jsx.jinja` wraps pages in a centered white column on the beige `bg-background`, and styles the `?state=` edge-state screens.
  > **Deterministic flow-action injection (the key fix).** `generate.py` builds a `flow_lookup` (`flow-step testId -> expectScreen`) from the AppModel's `flows`. Before rendering each component, `_inject_flow_actions()` gives any element that *lacks* a real action a synthetic `{type:'navigate', targetScreen}` when its `testId` matches a flow step (placeholder-aware: `story-link-{id}` ↔ `story-link-5`). So navigation is derived from the graph the reasoning stage is best at producing (flows), not from per-element `action` blocks it kept forgetting.
  > **Reasoning robustness.** Extended `verify_graph_integrity` to also reject element `action.targetScreen`s that don't resolve to a defined screen. Added a plain-English camelCase hint to `_format_error` for identifier `pattern` violations (Scout was thrashing 5 retries on snake_case field names like `comments_count`; with the hint it converges in ~1). Prompt (`prompts.py`) now: requires an `action` per element, mandates `{field}`-placeholder labels + a title-first display element on entity-bound components, and forbids satisfying integrity by *deleting* content.
* **Challenges & Solutions:**
  > **The 17B model is the ceiling; the generator must be robust to it.** Groq's Llama 4 Scout (free tier) cannot satisfy all AppModel constraints at once — each emphasis (title display / wired actions / valid nav / camelCase / edge states) makes it shed another. The stronger `qwen/qwen3.6-27b` is TPM-blocked on the free tier (our ~21K-token request — full schema + base64 screenshot — exceeds the 8K limit → HTTP 413). Resolution (user-chosen): stop depending on the LLM to wire interactions and **derive them deterministically in the generator from the flows graph**. Now a model that produces a correct *flow* but forgets element *actions* still yields a navigable mock.
* **Verification:**
  > Synthetic proof: a component whose title link had NO action but whose testId appeared in a flow step → generator injected `navigate(`/story/${props.id}`)`; an element absent from all flows stayed inert. Live: `mockbuilder build https://news.ycombinator.com -o ./out` (exit 0, 2 screens, 1 flow) produced a `StoryRow` where the model-provided `action` was `None` on the title link, yet the generated JSX wires `onClick={() => { navigate(`/story/${props.id}`); }}` from the flow — plus real story titles, comment counts, an upvote that mutates state, and clean styled rows. `App.jsx` routes `/`→Home and `/story/:id`→StoryDetail. **`npm install` clean + `npx vite build` succeeded (38 modules transformed, production CSS+JS bundles, built in 2.76s).**
  > Known residual (model, not generator): the model sometimes leaves a label empty (e.g. an upvote button with `""`) or omits an action on secondary elements (hide/comments), so those render but don't act. The generator wires everything the flows/actions declare; richer per-element behavior tracks reasoning-model quality (revisit with a stronger model after a Groq tier upgrade).

## Phase 4: The Verifier

### Date: 2026-07-03
* **Objective:** Turn the PLAN's core properties into a mechanical, pass/fail scorecard so a generated harness can prove — not merely claim — that it builds, runs offline, and is navigable. Scope: P8 (buildable), P1 (self-contained), P4 (navigable).
* **Implementation Notes:**
  > `mockbuilder/verifier/verify.py` — `run_verification(out_dir, app_model)` runs the real toolchain: `npm install` then `npm run build` (streamed to stdout) for **P8**. npm is resolved via `shutil.which` and its directory prepended to the subprocess PATH (so the `.cmd` shim finds `node` even in non-interactive shells). If the build fails, P1/P4 are skipped (nothing to run). Returns a scorecard dict `{P8, P1, P4, details}`.
  > `mockbuilder/verifier/agent_walk.py` — `walk_flows(out_dir, app_model)` serves `out_dir/dist` with `python -m http.server` on `127.0.0.1:4173` (trivial to tear down; client-side routing means we never deep-link, so no SPA fallback needed), then drives headless Chromium. **P1:** a `page.on("request")` listener flags any request whose host isn't `localhost`/`127.0.0.1`. **P4:** for each flow step it clicks `[data-testid^='<testId minus {id}>']` (prefix match so interpolated instances resolve) and asserts the `{expectScreen}-screen` div appears.
  > `cli.py` — `build` now calls `run_verification` right after generation, prints a `[PASS]/[FAIL]` scorecard, and writes `out_dir/scorecard.json`.
* **Challenges & Solutions:**
  > **Verification is executable, not heuristic.** Each property is proven by doing the thing: P8 actually compiles the project, P1 actually counts outbound requests from a real browser, P4 actually clicks through the flow graph and checks the destination rendered. A red property means the harness genuinely fails — there is no way to pass without the behavior being real. The screen `data-testid` convention (`{id}-screen`, emitted by the screen template) is what makes P4 a precise, generator-independent assertion target.
  > **Windows npm resolution.** `subprocess` couldn't launch `npm`/`npm.cmd` directly in a non-interactive shell (PATH quirks); resolving the absolute path with `shutil.which` and injecting its directory into the child PATH fixed it portably.
* **Verification:**
  > Full pipeline: `mockbuilder build https://news.ycombinator.com -o ./out` runs crawl → reason → generate → **verify** with `npm install` and `vite build` visible in stdout, then boots the preview server, walks the app, and prints a **GREEN** scorecard: `[PASS] P8 Build`, `[PASS] P1 Offline` (no external requests), `[PASS] P4 Navigable` (clicked `story-link-` → reached `story-detail-screen`). `scorecard.json` is written to the output dir. Standalone re-run against a pre-built `./out` produced the same all-green result.

### Date: 2026-07-06
* **Objective:** Context Budget Optimization — the reasoning request (~21K tokens: full schema + all scraped elements + base64 screenshot) was ~2-3x over the free tier's 8000 TPM ceiling, which forced a downgrade to the 17B Llama 4 Scout, whose weak instruction-following dropped nav links/fields and hallucinated seed rows. Slim the payload so the stronger Qwen 3 model fits.
* **Implementation Notes:**
  > **Elements compression (`compress_elements`).** Group discovered elements by a structural signature (`tag` + selector with `:nth-of-type(N)`/numbers normalized to wildcards); keep the first 3 of each group intact (enough to model a story/comment row as seed) and collapse the rest into one summary entry carrying the count. HN: 229 elements / 32.7KB -> 59 items / 5.2KB.
  > **Schema minification (`_minify_schema` + `MINIFIED_SCHEMA`).** Recursively strip `description` keys (~2/3 of the file, no machine constraint) and dump with compact separators: 15.7KB -> 5.2KB, every type/enum/required/pattern preserved.
  > **Dropped the vision payload.** Removed the base64 screenshot from the prompt entirely (the single biggest token cost — tens of thousands of vision tokens). The compressed elements + `design_tokens.json` carry the structural and stylistic facts, so reasoning is now TEXT-ONLY. Removed the `base64` import; the user message is a plain string, not an OpenAI vision content array.
  > **Stronger model.** With the request now ~3.7K tokens (no image), it fits the budget, so `MODEL` is back to `qwen/qwen3.6-27b` (Scout kept as a documented fallback).
* **Challenges & Solutions:**
  > **The bottleneck was budget, not architecture.** The evidence capture, design tokens, and generator were all fine — the model was simply starved of headroom. Fitting the request under 8K TPM (measured ~3,730 tokens vs the prior ~21K) is the single highest-leverage change: it swaps a 17B model with famously weak long-JSON adherence for a 27B one, without touching the pipeline. Compression is lossless where it matters (3 representative rows + exact counts) and lossy only on pure redundancy.
* **Verification:**
  > Measured offline before spending a call: elements 32.7KB->5.2KB, schema 15.7KB->5.2KB, total prompt ~14.6KB (~3,730 tokens, image dropped) — well under 8000 TPM. Live re-run below.

### Date: 2026-07-06 (cont.) — Budget fix outcome + convergence
* **Outcome (correcting the entry above):** The slimmed prompt worked, but `qwen/qwen3.6-27b` still failed on the 8000 TPM tier for two reasons: (1) it is a *reasoning* model — it emits a `<think>...</think>` preamble that isn't JSON and burns large output tokens (truncated JSON); (2) retries *accumulate* conversation tokens, and once a single request exceeds 8000 it can't succeed no matter how long we wait (`Requested 10497`). **Key realization: dropping the screenshot means we no longer need a *vision* model.** Switched `MODEL` to `llama-3.3-70b-versatile` — Groq's strongest general *text* model, non-reasoning (clean JSON, no `<think>`), far better instruction-following than the 17B Scout, and it fits the text-only budget.
* **Supporting fixes for the tight budget / stronger model:**
  > `_strip_json_fences` also removes `<think>...</think>` (harmless for non-reasoning models; safety for any reasoning model). `_create_completion` wraps the call with a 60s back-off on 413/429. The validate-retry loop now rebuilds a **bounded** conversation each attempt (fixed prefix + latest attempt + error) instead of accumulating, so retry requests never grow past the ceiling.
  > **Extended referential-integrity gate:** the richer llama-3.3-70b models place components in screen `layout.regions` that they hadn't defined (`Could not resolve "../components/Navbar"`), breaking the build. `verify_graph_integrity` now also rejects screen regions that reference an undefined component — so the retry loop forces the model to *define* them (it did: components 2 -> 4). The generator (`Screen.jsx.jinja` + `generate_screens`) also filters region components to defined ones as defense-in-depth.
  > **Verifier P4 fix (multi-start flows):** the stronger model produces flows that start on a non-home screen (e.g. `story-to-home`). `agent_walk` now serves `dist/` via an in-process threaded static server *with SPA fallback* and navigates to each flow's first screen (deep-linking `/story/1`) before walking — instead of assuming every flow starts at home.
* **Verification (live):** `mockbuilder build https://news.ycombinator.com -o ./out` now yields a **GREEN** scorecard — `[PASS] P8 Build`, `[PASS] P1 Offline`, `[PASS] P4 Navigable` — on a richer AppModel (`components=4, screens=2, flows=2`, e.g. `home-to-story` and `story-to-home`) that builds, runs offline, and is fully navigable both directions. Measured prompt ~3.7K tokens (down from ~21K) fits the 8000 TPM free tier.

### Date: 2026-07-06 — Reality check: GREEN scorecard, broken mock
* **Observation (user-reported, with screenshot):** Despite an all-GREEN scorecard, the rendered home page shows only the `Navbar` ("new  past"), a literal **"undefined"**, and the `FooterNav` ("Guidelines  FAQ"). There are **no story articles**, and clicking does nothing useful. The mock is not usable.
* **Root cause — list rendering is a generator/model contract mismatch.** The model created `StoryList` (a component bound to the `story` entity, with a `story-link-{id}` element labelled `{title}`) and placed it in the home screen's `main` region — but that region is **not** marked `repeats`/`repeatsOver`. The generator only iterates seed data when a region is `repeats: true` (mapping the entity's rows to one component instance each). A non-repeating region renders the component exactly **once, with no props**, so `props.title` is `undefined` -> the page literally prints "undefined", and the 20 seeded stories are never displayed. In short: the model models "a list" as a single list-component; the generator only knows how to render lists as a *repeating row component*. Neither is wrong alone; together they produce nothing.
* **Why the scorecard was still GREEN (the real lesson):** P8/P1/P4 verify *build succeeds*, *no external network*, and *a data-testid-prefixed element is clickable and changes screen* — none of which assert that **real seed content actually renders**. P4 "passed" by clicking the single broken `story-link-undefined` link. The verifier has no fidelity/content property (PLAN's P2 "faithful" / P5 "populated" aren't implemented), so a mock showing "undefined" and zero articles sails through green. **A green scorecard currently means "compiles, offline, and the flow graph is wired" — not "looks right" or "shows the data."**
* **Fix directions (not yet applied):**
  > 1. **Generator:** when an entity-bound component sits in a content region (e.g. `main`) without explicit `repeats`, auto-iterate it over its bound entity's seed (treat `boundToEntity` as an implicit `repeatsOver`). This makes list rendering robust to how the model expresses it.
  > 2. **Gate/prompt:** require that an entity-bound "list/row" component be placed in a `repeats: true` region (or forbid the model from placing a bound component in a non-repeating region), so the data path is always explicit.
  > 3. **Verifier:** add a content-fidelity property — assert the rendered page contains actual seed values (e.g. a known story title) and that no literal "undefined" appears — so this class of failure turns the scorecard RED instead of green.
* **Status:** honestly, the pipeline now produces a *buildable, offline, wired* harness but not a *faithful, populated* one on this run. The budget/model work (llama-3.3-70b) materially improved the AppModel richness; the remaining gap is the list-rendering contract and the verifier's blindness to content — both tracked above for the next iteration.
