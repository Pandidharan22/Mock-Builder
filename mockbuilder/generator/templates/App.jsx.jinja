{# Auto-generated by MockBuilder from the AppModel. Do not edit by hand. #}
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { GlobalProvider } from './GlobalContext';
{% for screen in screens %}
import {{ screen.id.split('-') | map('capitalize') | join }} from './screens/{{ screen.id }}';
{% endfor %}

export default function App() {
  return (
    <GlobalProvider>
      <BrowserRouter>
        <Routes>
{% for screen in screens %}
          <Route path="{{ screen.route | route_path }}" element={<{{ screen.id.split('-') | map('capitalize') | join }} />} />
{% endfor %}
        </Routes>
      </BrowserRouter>
    </GlobalProvider>
  );
}
